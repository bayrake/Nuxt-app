# Test environment (used for unit/integration tests)
# Points to JSONPlaceholder by default so you can run tests against a public API
NUXT_PUBLIC_API_BASE=https://jsonplaceholder.typicode.com
NODE_ENV=test
